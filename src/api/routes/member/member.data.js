import use from 'abrequire';
import { connection } from '../../../db/dbconnect.js';
import { DB_STATUS } from '../../../constants/constants.js';


export default {
  // 회원가입
  memberJoin_db: async (param) => {},
  // 회원탈퇴
  memberOut_db: async (param) => {},
  // 로그인
  memberLogin_db: async (param) => {},
  // 로그아웃
  memberLogout_db: async (param) => {},
  // 알림설정
  memberAlarm_db: async(param) => {}
  
};